import { useParams } from "react-router-dom";

const LandDetails = () => {

    const { id } = useParams()
    return (
        <div>
            
        </div>
    );
};

export default LandDetails;