import UseTitle from "../Title/UseTitle";

const Achievement = () => {
    UseTitle("Achievement")
    
    return (
        <div>
            achievement
        </div>
    );
};

export default Achievement;